## Getting Started

First, install the project dependeces and husky git hooks:

```bash
yarn install
```
Then, run the developemnt server with:

```bash
yarn dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Tests and Lints

All the test and linters will run automatically when a commit is made.
